name: CI

on:
  push:
    branches: [main, devel]
  pull_request:
    branches: [main, devel]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: stable
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: latest

  test:
    name: Unit tests (Go ${{ matrix.go-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26', 'stable']
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Install gotestsum
        # gotestsum@latest's go.mod may require a newer toolchain than the
        # matrix Go; allow this build step (only) to fetch one. The tests
        # below still run on the matrix Go version.
        env:
          GOTOOLCHAIN: auto
        run: go install gotest.tools/gotestsum@latest
      - name: Run unit tests
        run: gotestsum -- ./... -race -covermode=atomic -count=1 -coverprofile=coverage.txt
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: peczenyj/runtimevar-consul

  integration:
    name: Integration tests (Go ${{ matrix.go-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: ['1.26', 'stable']
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Install gotestsum
        # See note above: only the tool build may upgrade the toolchain.
        env:
          GOTOOLCHAIN: auto
        run: go install gotest.tools/gotestsum@latest
      - name: Run integration tests
        run: gotestsum -- -tags=integration ./...
